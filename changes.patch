Submodule subprojects/frida-core contains modified content
diff --git a/subprojects/frida-core/lib/base/session.vala b/subprojects/frida-core/lib/base/session.vala
index feb88aa..29e623a 100644
--- a/subprojects/frida-core/lib/base/session.vala
+++ b/subprojects/frida-core/lib/base/session.vala
@@ -1,5 +1,5 @@
 namespace Frida {
-	[DBus (name = "re.frida.HostSession17")]
+	[DBus (name = "re.grik.HostSession17")]
 	public interface HostSession : Object {
 		public abstract async void ping (uint interval_seconds, Cancellable? cancellable) throws GLib.Error;
 
@@ -43,7 +43,7 @@ namespace Frida {
 		public signal void uninjected (InjectorPayloadId id);
 	}
 
-	[DBus (name = "re.frida.AgentSessionProvider17")]
+	[DBus (name = "re.grik.AgentSessionProvider17")]
 	public interface AgentSessionProvider : Object {
 		public abstract async void open (AgentSessionId id, HashTable<string, Variant> options,
 			Cancellable? cancellable) throws GLib.Error;
@@ -58,7 +58,7 @@ namespace Frida {
 		public signal void child_gating_changed (uint subscriber_count);
 	}
 
-	[DBus (name = "re.frida.AgentSession17")]
+	[DBus (name = "re.grik.AgentSession17")]
 	public interface AgentSession : Object {
 		public abstract async void close (Cancellable? cancellable) throws GLib.Error;
 
@@ -100,7 +100,7 @@ namespace Frida {
 		public signal void candidate_gathering_done ();
 	}
 
-	[DBus (name = "re.frida.AgentController17")]
+	[DBus (name = "re.grik.AgentController17")]
 	public interface AgentController : Object {
 #if !WINDOWS
 		public abstract async HostChildId prepare_to_fork (uint parent_pid, Cancellable? cancellable, out uint parent_injectee_id,
@@ -121,7 +121,7 @@ namespace Frida {
 			Cancellable? cancellable) throws GLib.Error;
 	}
 
-	[DBus (name = "re.frida.AgentMessageSink17")]
+	[DBus (name = "re.grik.AgentMessageSink17")]
 	public interface AgentMessageSink : Object {
 		public abstract async void post_messages (AgentMessage[] messages, uint batch_id,
 			Cancellable? cancellable) throws GLib.Error;
@@ -749,7 +749,7 @@ namespace Frida {
 		}
 	}
 
-	[DBus (name = "re.frida.GadgetSession17")]
+	[DBus (name = "re.grik.GadgetSession17")]
 	public interface GadgetSession : Object {
 		public abstract async void break_and_resume (Cancellable? cancellable) throws GLib.Error;
 		public abstract async void break_and_detach (Cancellable? cancellable) throws GLib.Error;
@@ -762,14 +762,14 @@ namespace Frida {
 		PAGE_PLAN,
 	}
 
-	[DBus (name = "re.frida.Channel17")]
+	[DBus (name = "re.grik.Channel17")]
 	public interface Channel : Object {
 		public abstract async void close (Cancellable? cancellable) throws GLib.Error;
 		public abstract async void input (uint8[] data, Cancellable? cancellable) throws GLib.Error;
 		public signal void output (uint8[] data);
 	}
 
-	[DBus (name = "re.frida.ServiceSession17")]
+	[DBus (name = "re.grik.ServiceSession17")]
 	public interface ServiceSession : Object {
 		public signal void close ();
 		public signal void message (Variant message);
@@ -779,13 +779,13 @@ namespace Frida {
 		public abstract async Variant request (Variant parameters, Cancellable? cancellable) throws GLib.Error;
 	}
 
-	[DBus (name = "re.frida.TransportBroker17")]
+	[DBus (name = "re.grik.TransportBroker17")]
 	public interface TransportBroker : Object {
 		public abstract async void open_tcp_transport (AgentSessionId id, Cancellable? cancellable, out uint16 port,
 			out string token) throws GLib.Error;
 	}
 
-	[DBus (name = "re.frida.PortalSession17")]
+	[DBus (name = "re.grik.PortalSession17")]
 	public interface PortalSession : Object {
 		public abstract async void join (HostApplicationInfo app, SpawnStartState current_state,
 			AgentSessionId[] interrupted_sessions, HashTable<string, Variant> options, Cancellable? cancellable,
@@ -794,14 +794,14 @@ namespace Frida {
 		public signal void kill ();
 	}
 
-	[DBus (name = "re.frida.BusSession17")]
+	[DBus (name = "re.grik.BusSession17")]
 	public interface BusSession : Object {
 		public abstract async void attach (Cancellable? cancellable) throws GLib.Error;
 		public abstract async void post (string json, bool has_data, uint8[] data, Cancellable? cancellable) throws GLib.Error;
 		public signal void message (string json, bool has_data, uint8[] data);
 	}
 
-	[DBus (name = "re.frida.AuthenticationService17")]
+	[DBus (name = "re.grik.AuthenticationService17")]
 	public interface AuthenticationService : Object {
 		public abstract async string authenticate (string token, Cancellable? cancellable) throws GLib.Error;
 	}
@@ -1047,7 +1047,7 @@ namespace Frida {
 		}
 	}
 
-	[DBus (name = "re.frida.Error")]
+	[DBus (name = "re.grik.Error")]
 	public errordomain Error {
 		SERVER_NOT_RUNNING,
 		EXECUTABLE_NOT_FOUND,
@@ -1086,7 +1086,7 @@ namespace Frida {
 			throw (IOError) e;
 
 		if (e is DBusError.UNKNOWN_METHOD) {
-			throw new Frida.Error.PROTOCOL ("Unable to communicate with remote frida-server; " +
+			throw new Frida.Error.PROTOCOL ("Unable to communicate with remote grik-server; " +
 				"please ensure that major versions match and that the remote Frida has the " +
 				"feature you are trying to use");
 		}
diff --git a/subprojects/frida-core/lib/base/socket.vala b/subprojects/frida-core/lib/base/socket.vala
index d3cc48f..d34ce29 100644
--- a/subprojects/frida-core/lib/base/socket.vala
+++ b/subprojects/frida-core/lib/base/socket.vala
@@ -1,5 +1,5 @@
 namespace Frida {
-	public const uint16 DEFAULT_CONTROL_PORT = 27042;
+	public const uint16 DEFAULT_CONTROL_PORT = 37042;
 	public const uint16 DEFAULT_CLUSTER_PORT = 27052;
 
 	public SocketConnectable parse_control_address (string? address, uint16 port = 0) throws Error {
diff --git a/subprojects/frida-core/lib/gadget/gadget.vala b/subprojects/frida-core/lib/gadget/gadget.vala
index 230284c..a02781e 100644
--- a/subprojects/frida-core/lib/gadget/gadget.vala
+++ b/subprojects/frida-core/lib/gadget/gadget.vala
@@ -1724,7 +1724,7 @@ namespace Frida.Gadget {
 				}
 
 				uint pid = get_process_id ();
-				string identifier = "re.frida.Gadget";
+				string identifier = "re.grik.Gadget";
 				string name = "Gadget";
 				var no_parameters = make_parameters_dict ();
 				this_app = HostApplicationInfo (identifier, name, pid, no_parameters);
diff --git a/subprojects/frida-core/lib/netif/tunnel-interface-observer.vala b/subprojects/frida-core/lib/netif/tunnel-interface-observer.vala
index 4325d0a..5c98e17 100644
--- a/subprojects/frida-core/lib/netif/tunnel-interface-observer.vala
+++ b/subprojects/frida-core/lib/netif/tunnel-interface-observer.vala
@@ -4,7 +4,7 @@ public sealed class Frida.TunnelInterfaceObserver : Object, DynamicInterfaceObse
 
 	private Darwin.SystemConfiguration.DynamicStore? store;
 	private Darwin.GCD.DispatchQueue event_queue =
-		new Darwin.GCD.DispatchQueue ("re.frida.endpoint-enumerator", Darwin.GCD.DispatchQueueAttr.SERIAL);
+		new Darwin.GCD.DispatchQueue ("re.grik.endpoint-enumerator", Darwin.GCD.DispatchQueueAttr.SERIAL);
 
 	private MainContext? main_context;
 
diff --git a/subprojects/frida-core/server/server.vala b/subprojects/frida-core/server/server.vala
index 67bf4b3..2aa637a 100644
--- a/subprojects/frida-core/server/server.vala
+++ b/subprojects/frida-core/server/server.vala
@@ -1,7 +1,7 @@
 namespace Frida.Server {
 	private static Application application;
 
-	private const string DEFAULT_DIRECTORY = "re.frida.server";
+	private const string DEFAULT_DIRECTORY = "re.grik.server";
 	private static bool output_version = false;
 	private static string? device_id = null;
 	private static string? listen_address = null;
@@ -179,7 +179,7 @@ namespace Frida.Server {
 		Environment.configure ();
 
 #if DARWIN
-		var worker = new Thread<int> ("frida-server-main-loop", () => {
+		var worker = new Thread<int> ("grik-server-main-loop", () => {
 			var exit_code = run_application (device_id, endpoint_params, options, on_ready);
 
 			_stop_run_loop ();
diff --git a/subprojects/frida-core/src/barebone/qmp-client.vala b/subprojects/frida-core/src/barebone/qmp-client.vala
index b2d5ea9..0826a3f 100644
--- a/subprojects/frida-core/src/barebone/qmp-client.vala
+++ b/subprojects/frida-core/src/barebone/qmp-client.vala
@@ -127,7 +127,7 @@ namespace Frida.Barebone {
 			string chardev = "vserial0";
 			yield add_chardev_from_fd (chardev, fd_name, cancellable);
 
-			yield add_serial_port (chardev, bus, "re.frida.hostlink", "hostlink.port", 1, cancellable);
+			yield add_serial_port (chardev, bus, "re.grik.hostlink", "hostlink.port", 1, cancellable);
 
 			return new Hostlink () {
 				connection = SocketConnection.factory_create_connection (local_sock),
diff --git a/subprojects/frida-core/src/darwin/frida-helper-process.vala b/subprojects/frida-core/src/darwin/frida-helper-process.vala
index 76bb2c7..0180cdc 100644
--- a/subprojects/frida-core/src/darwin/frida-helper-process.vala
+++ b/subprojects/frida-core/src/darwin/frida-helper-process.vala
@@ -256,7 +256,7 @@ namespace Frida {
 		private static void throw_helper_error (GLib.Error e) throws Error, IOError {
 #if MACOS
 			if (e is IOError.CLOSED) {
-				throw new Error.PERMISSION_DENIED ("Oops, frida-helper appears to have crashed. It may have been killed " +
+				throw new Error.PERMISSION_DENIED ("Oops, grik-helper appears to have crashed. It may have been killed " +
 					"by the system while trying to access a hardened process. If this is the case, try setting these " +
 					"boot arguments: `sudo nvram boot-args=\"-arm64e_preview_abi thid_should_crash=0 " +
 					"tss_should_crash=0\"`. For more information, see: https://github.com/frida/frida-core/issues/524");
@@ -507,7 +507,7 @@ namespace Frida {
 
 			var blob = Frida.Data.Helper.get_frida_helper_blob ();
 			helper = new TemporaryHelperFile (
-				new TemporaryFile.from_stream ("frida-helper",
+				new TemporaryFile.from_stream ("grik-helper",
 					new MemoryInputStream.from_data (blob.data, null),
 					tempdir));
 			FileUtils.chmod (helper.path, 0700);
diff --git a/subprojects/frida-core/src/darwin/frida-helper-service.vala b/subprojects/frida-core/src/darwin/frida-helper-service.vala
index 80a7640..e808cca 100644
--- a/subprojects/frida-core/src/darwin/frida-helper-service.vala
+++ b/subprojects/frida-core/src/darwin/frida-helper-service.vala
@@ -5,7 +5,7 @@ namespace Frida {
 		Gum.init ();
 
 		var parent_address = args[1];
-		var worker = new Thread<int> ("frida-helper-main-loop", () => {
+		var worker = new Thread<int> ("grik-helper-main-loop", () => {
 			var service = new DarwinHelperService (parent_address);
 
 			var exit_code = service.run ();
diff --git a/subprojects/frida-core/src/darwin/frida-helper-types.vala b/subprojects/frida-core/src/darwin/frida-helper-types.vala
index 4a8309d..3a40f1b 100644
--- a/subprojects/frida-core/src/darwin/frida-helper-types.vala
+++ b/subprojects/frida-core/src/darwin/frida-helper-types.vala
@@ -46,7 +46,7 @@ namespace Frida {
 		public abstract async MappedLibraryBlob? try_mmap (Bytes blob, Cancellable? cancellable) throws Error, IOError;
 	}
 
-	[DBus (name = "re.frida.Helper")]
+	[DBus (name = "re.grik.Helper")]
 	public interface DarwinRemoteHelper : Object {
 		public signal void output (uint pid, int fd, uint8[] data);
 		public signal void spawn_added (HostSpawnInfo info);
diff --git a/subprojects/frida-core/src/droidy/droidy-host-session.vala b/subprojects/frida-core/src/droidy/droidy-host-session.vala
index 0835b94..cd71f80 100644
--- a/subprojects/frida-core/src/droidy/droidy-host-session.vala
+++ b/subprojects/frida-core/src/droidy/droidy-host-session.vala
@@ -272,7 +272,7 @@ namespace Frida {
 		private Cancellable io_cancellable = new Cancellable ();
 
 		private const double MIN_SERVER_CHECK_INTERVAL = 5.0;
-		private const string GADGET_APP_ID = "re.frida.Gadget";
+		private const string GADGET_APP_ID = "re.grik.Gadget";
 
 		public DroidyHostSession (Droidy.DeviceDetails device_details, HostChannelProvider channel_provider,
 				string control_endpoint) {
@@ -452,7 +452,7 @@ namespace Frida {
 				if (opts.has_selected_identifiers ()) {
 					gadget_is_selected = false;
 					opts.enumerate_selected_identifiers (identifier => {
-						if (identifier == "re.frida.Gadget")
+						if (identifier == "re.grik.Gadget")
 							gadget_is_selected = true;
 					});
 				}
@@ -883,7 +883,7 @@ namespace Frida {
 				}
 
 				if (connection.closed)
-					throw new Error.SERVER_NOT_RUNNING ("Unable to connect to remote frida-server");
+					throw new Error.SERVER_NOT_RUNNING ("Unable to connect to remote grik-server");
 
 				var server = new RemoteServer (session, connection, flavor, transport_broker);
 				attach_remote_server (server);
@@ -904,11 +904,11 @@ namespace Frida {
 					last_server_check_error = null;
 				} else {
 					if (e is Error.SERVER_NOT_RUNNING) {
-						api_error = new Error.SERVER_NOT_RUNNING ("Unable to connect to remote frida-server");
+						api_error = new Error.SERVER_NOT_RUNNING ("Unable to connect to remote grik-server");
 					} else if (connection != null) {
-						api_error = new Error.PROTOCOL ("Incompatible frida-server version");
+						api_error = new Error.PROTOCOL ("Incompatible grik-server version");
 					} else {
-						api_error = new Error.SERVER_NOT_RUNNING ("Unable to connect to remote frida-server: %s",
+						api_error = new Error.SERVER_NOT_RUNNING ("Unable to connect to remote grik-server: %s",
 							e.message);
 					}
 
@@ -1023,7 +1023,7 @@ namespace Frida {
 			try {
 				string device_serial = device_details.serial;
 				string instance_id = Uuid.string_random ().replace ("-", "");
-				string helper_path = "/data/local/tmp/frida-helper-" + instance_id + ".dex";
+				string helper_path = "/data/local/tmp/grik-helper-" + instance_id + ".dex";
 
 				var helper_dex = new MemoryInputStream.from_bytes (
 					new Bytes.static (Frida.Data.Android.get_helper_dex_blob ().data));
@@ -1050,8 +1050,8 @@ namespace Frida {
 
 					shell.send_command (("CLASSPATH=%s app_process " +
 							"/data/local/tmp " +
-							"--nice-name=re.frida.helper " +
-							"re.frida.Helper " +
+							"--nice-name=re.grik.helper " +
+							"re.grik.Helper " +
 							"%s; " +
 							"rm -f %s; " +
 							"echo BYE.").printf (helper_path, instance_id, helper_path));
@@ -1071,7 +1071,7 @@ namespace Frida {
 				var client = yield Droidy.Client.open (cancellable);
 				try {
 					yield client.request ("host:transport:" + device_serial, cancellable);
-					yield client.request_protocol_change ("localabstract:/frida-helper-" + instance_id, cancellable);
+					yield client.request_protocol_change ("localabstract:/grik-helper-" + instance_id, cancellable);
 				} catch (GLib.Error e) {
 					client.close.begin ();
 					throw e;
diff --git a/subprojects/frida-core/src/fruity/device-monitor-macos.vala b/subprojects/frida-core/src/fruity/device-monitor-macos.vala
index 063af22..9c670bc 100644
--- a/subprojects/frida-core/src/fruity/device-monitor-macos.vala
+++ b/subprojects/frida-core/src/fruity/device-monitor-macos.vala
@@ -8,7 +8,7 @@ namespace Frida.Fruity {
 
 		private XpcClient? pairingd;
 		private Darwin.GCD.DispatchQueue queue =
-			new Darwin.GCD.DispatchQueue ("re.frida.fruity.remotepairing", Darwin.GCD.DispatchQueueAttr.SERIAL);
+			new Darwin.GCD.DispatchQueue ("re.grik.fruity.remotepairing", Darwin.GCD.DispatchQueueAttr.SERIAL);
 
 		public async void start (Cancellable? cancellable) throws IOError {
 			pairingd = XpcClient.make_for_mach_service ("com.apple.CoreDevice.remotepairingd", queue);
diff --git a/subprojects/frida-core/src/fruity/fruity-host-session.vala b/subprojects/frida-core/src/fruity/fruity-host-session.vala
index 6374a7e..8cf36c2 100644
--- a/subprojects/frida-core/src/fruity/fruity-host-session.vala
+++ b/subprojects/frida-core/src/fruity/fruity-host-session.vala
@@ -214,7 +214,7 @@ namespace Frida {
 		private Cancellable io_cancellable = new Cancellable ();
 
 		private const double MIN_SERVER_CHECK_INTERVAL = 5.0;
-		private const string GADGET_APP_ID = "re.frida.Gadget";
+		private const string GADGET_APP_ID = "re.grik.Gadget";
 		private const string DEBUGSERVER_ENDPOINT_17PLUS = "com.apple.internal.dt.remote.debugproxy";
 		private const string DEBUGSERVER_ENDPOINT_14PLUS = "com.apple.debugserver.DVTSecureSocketProxy";
 		private const string DEBUGSERVER_ENDPOINT_LEGACY = "com.apple.debugserver?tls=handshake-only";
@@ -1333,7 +1333,7 @@ namespace Frida {
 				}
 
 				if (connection.closed)
-					throw new Error.SERVER_NOT_RUNNING ("Unable to connect to remote frida-server");
+					throw new Error.SERVER_NOT_RUNNING ("Unable to connect to remote grik-server");
 
 				var server = new RemoteServer (flavor, session, connection, channel, device, transport_broker);
 				attach_remote_server (server);
@@ -1356,9 +1356,9 @@ namespace Frida {
 					if (e is Error) {
 						api_error = e;
 					} else if (connection != null) {
-						api_error = new Error.PROTOCOL ("Incompatible frida-server version");
+						api_error = new Error.PROTOCOL ("Incompatible grik-server version");
 					} else {
-						api_error = new Error.SERVER_NOT_RUNNING ("Unable to connect to remote frida-server: %s",
+						api_error = new Error.SERVER_NOT_RUNNING ("Unable to connect to remote grik-server: %s",
 							e.message);
 					}
 
diff --git a/subprojects/frida-core/src/linux/frida-helper-process.vala b/subprojects/frida-core/src/linux/frida-helper-process.vala
index 2e92db0..3b6aeaf 100644
--- a/subprojects/frida-core/src/linux/frida-helper-process.vala
+++ b/subprojects/frida-core/src/linux/frida-helper-process.vala
@@ -644,11 +644,11 @@ namespace Frida {
 #if HAVE_EMBEDDED_ASSETS
 			var blob32 = Frida.Data.Helper.get_frida_helper_32_blob ();
 			if (blob32.data.length > 0)
-				helper32 = make_temporary_helper ("frida-helper-32", blob32.data);
+				helper32 = make_temporary_helper ("grik-helper-32", blob32.data);
 
 			var blob64 = Frida.Data.Helper.get_frida_helper_64_blob ();
 			if (blob64.data.length > 0)
-				helper64 = make_temporary_helper ("frida-helper-64", blob64.data);
+				helper64 = make_temporary_helper ("grik-helper-64", blob64.data);
 #else
 			var tpl = PathTemplate (Frida.helper_path);
 			string path = tpl.expand ((sizeof (void *) == 8) ? "32" : "64");
diff --git a/subprojects/frida-core/src/linux/frida-helper-types.vala b/subprojects/frida-core/src/linux/frida-helper-types.vala
index fee8ae8..a385b2b 100644
--- a/subprojects/frida-core/src/linux/frida-helper-types.vala
+++ b/subprojects/frida-core/src/linux/frida-helper-types.vala
@@ -42,7 +42,7 @@ namespace Frida {
 		CONTROL_CHANNEL,
 	}
 
-	[DBus (name = "re.frida.Helper")]
+	[DBus (name = "re.grik.Helper")]
 	public interface LinuxRemoteHelper : Object {
 		public signal void output (uint pid, int fd, uint8[] data);
 		public signal void uninjected (uint id);
diff --git a/subprojects/frida-core/src/linux/linux-host-session.vala b/subprojects/frida-core/src/linux/linux-host-session.vala
index 3a500c3..573b485 100644
--- a/subprojects/frida-core/src/linux/linux-host-session.vala
+++ b/subprojects/frida-core/src/linux/linux-host-session.vala
@@ -462,7 +462,7 @@ namespace Frida {
 
 			try {
 				string instance_id = Uuid.string_random ().replace ("-", "");
-				string helper_path = "/data/local/tmp/frida-helper-" + instance_id + ".dex";
+				string helper_path = "/data/local/tmp/grik-helper-" + instance_id + ".dex";
 				FileUtils.set_data (helper_path, Frida.Data.Android.get_helper_dex_blob ().data);
 				Posix.chmod (helper_path, 0644);
 
diff --git a/subprojects/frida-core/src/socket/socket-host-session.vala b/subprojects/frida-core/src/socket/socket-host-session.vala
index d18fa33..63b060e 100644
--- a/subprojects/frida-core/src/socket/socket-host-session.vala
+++ b/subprojects/frida-core/src/socket/socket-host-session.vala
@@ -99,9 +99,9 @@ namespace Frida {
 				socket_connection = yield client.connect_async (connectable, cancellable);
 			} catch (GLib.Error e) {
 				if (e is IOError.CONNECTION_REFUSED)
-					throw new Error.SERVER_NOT_RUNNING ("Unable to connect to remote frida-server");
+					throw new Error.SERVER_NOT_RUNNING ("Unable to connect to remote grik-server");
 				else
-					throw new Error.SERVER_NOT_RUNNING ("Unable to connect to remote frida-server: %s", e.message);
+					throw new Error.SERVER_NOT_RUNNING ("Unable to connect to remote grik-server: %s", e.message);
 			}
 
 			Socket socket = socket_connection.socket;
@@ -151,7 +151,7 @@ namespace Frida {
 					auth_service = yield connection.get_proxy (null, ObjectPath.AUTHENTICATION_SERVICE,
 						DO_NOT_LOAD_PROPERTIES, cancellable);
 				} catch (IOError e) {
-					throw new Error.PROTOCOL ("Incompatible frida-server version");
+					throw new Error.PROTOCOL ("Incompatible grik-server version");
 				}
 
 				try {
@@ -166,7 +166,7 @@ namespace Frida {
 				host_session = yield connection.get_proxy (null, ObjectPath.HOST_SESSION, DO_NOT_LOAD_PROPERTIES,
 					cancellable);
 			} catch (IOError e) {
-				throw new Error.PROTOCOL ("Incompatible frida-server version");
+				throw new Error.PROTOCOL ("Incompatible grik-server version");
 			}
 
 			var entry = new HostEntry (connection, host_session, keepalive_interval);
diff --git a/subprojects/frida-core/src/windows/frida-helper-process.vala b/subprojects/frida-core/src/windows/frida-helper-process.vala
index 12ce73c..27fd39f 100644
--- a/subprojects/frida-core/src/windows/frida-helper-process.vala
+++ b/subprojects/frida-core/src/windows/frida-helper-process.vala
@@ -159,7 +159,7 @@ namespace Frida {
 
 				stream_request = Pipe.open (transport.local_address, cancellable);
 
-				new Thread<bool> ("frida-helper-factory", obtain_worker);
+				new Thread<bool> ("grik-helper-factory", obtain_worker);
 			}
 
 			return yield obtain_request.future.wait_async (cancellable);
@@ -404,7 +404,7 @@ namespace Frida {
 
 #if HAVE_EMBEDDED_ASSETS
 		private TemporaryFile add_helper (string name, Frida.Data.Helper.Blob blob, TemporaryDirectory tempdir) throws Error {
-			var file = new TemporaryFile.from_stream (@"frida-helper-$name.exe",
+			var file = new TemporaryFile.from_stream (@"grik-helper-$name.exe",
 				new MemoryInputStream.from_data (blob.data, null),
 				tempdir);
 			helpers.add (file);
diff --git a/subprojects/frida-core/src/windows/frida-helper-types.vala b/subprojects/frida-core/src/windows/frida-helper-types.vala
index 0d41020..75d9a07 100644
--- a/subprojects/frida-core/src/windows/frida-helper-types.vala
+++ b/subprojects/frida-core/src/windows/frida-helper-types.vala
@@ -8,7 +8,7 @@ namespace Frida {
 			string[] dependencies, uint id, Cancellable? cancellable) throws Error, IOError;
 	}
 
-	[DBus (name = "re.frida.Helper")]
+	[DBus (name = "re.grik.Helper")]
 	public interface WindowsRemoteHelper : Object {
 		public signal void uninjected (uint id);
 
diff --git a/subprojects/frida-core/tests/test-host-session.vala b/subprojects/frida-core/tests/test-host-session.vala
index c2c5c9f..5de5ae4 100644
--- a/subprojects/frida-core/tests/test-host-session.vala
+++ b/subprojects/frida-core/tests/test-host-session.vala
@@ -3613,7 +3613,7 @@ namespace Frida.HostSessionTest {
 			try {
 				Cancellable? cancellable = null;
 
-				stdout.printf ("connecting to frida-server\n");
+				stdout.printf ("connecting to grik-server\n");
 				var host_session = yield prov.create (new NullHostSessionHub (), null, cancellable);
 				stdout.printf ("enumerating processes\n");
 				var processes = yield host_session.enumerate_processes (make_parameters_dict (), cancellable);
